# Core
opencv-python>=4.8.0
numpy>=1.23.0
psutil>=5.9.0
requests>=2.31.0

# Inference (prefer ONNX Runtime; PyTorch optional)
onnxruntime>=1.16.0

# Optional GPU/accelerators (install manually if needed to avoid package conflicts):
# - CUDA GPU: pip install onnxruntime-gpu==<matching version>
# - Windows GPU via DirectML: pip install onnxruntime-directml==<matching version>
# - Intel OpenVINO EP: install OpenVINO toolkit and initialize env (see README)
# - PyTorch (CPU/GPU): pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# GUI
PyQt6>=6.5.0

# Video writing
imageio>=2.31.0
imageio-ffmpeg>=0.4.9

# Testing
pytest>=7.4.0
